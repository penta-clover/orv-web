export interface Video {
    id: string,
    storyboardId: string,
    memberId: string,
    videoUrl: string,
    createdAt: Date,
    thumbnailUrl: string,
    runningTime: number,
    title: string
}